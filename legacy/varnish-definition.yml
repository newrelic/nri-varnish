name: com.newrelic.varnish
description: Reports status and metrics for nri-varnish service
protocol_version: 2
os: linux

commands:
  all_data:
    command:
      - ./bin/nri-varnish
    interval: 15
    prefix: config/varnish
