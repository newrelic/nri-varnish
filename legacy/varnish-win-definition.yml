name: com.newrelic.varnish
description: Reports status and metrics for nr-varnish service. Legacy file kept for compatibility.
protocol_version: 2
os: windows

commands:
  all_data:
    command:
      - .\bin\nri-varnish.exe
    interval: 15
    prefix: config/varnish
